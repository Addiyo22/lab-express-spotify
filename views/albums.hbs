<h1>Albums for : {{artistName}}</h1>
{{#each albums}}
    <div class="album">
      <h2>{{name}}</h2>
      <img src="{{images.[0].url}}" alt="{{name}}'s album cover">
      <a href="/tracks/{{id}}">View Songs</a>
      </div>
      {{/each}}